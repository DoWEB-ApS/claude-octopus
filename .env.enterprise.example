# Copy this file to `.env.enterprise` and populate secrets locally.
# Do not commit real credentials.

# OAuth-first setup:
# - Leave keys blank if your team uses subscription OAuth.
# - Run `codex login` and `gemini` inside the container.

# OpenAI Codex (optional when using OAuth)
OPENAI_API_KEY=

# Gemini CLI (optional when using OAuth)
GEMINI_API_KEY=

# Optional: OpenRouter
OPENROUTER_API_KEY=

# Enterprise behavior toggles
# Keep false in production use.
DOWEB_ALLOW_HOST=false

# Auto-install /doweb Claude plugin at container startup (recommended).
DOWEB_AUTO_PLUGIN_INSTALL=true

# Optional: default autonomous mode for scripted sessions
CLAUDE_OCTOPUS_AUTONOMY=semi-autonomous
